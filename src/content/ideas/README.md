# Ideas Articles

**Note: This folder is deprecated. Please use `public/content/ideas/` instead.**

Markdown files are now served from the `public` folder so they can be added without rebuilding the site.

See `public/content/ideas/README.md` for instructions.

